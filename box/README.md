# box/ — JanuaPort Box

Die Box-Provisionierung ist hier bewusst **nicht** dupliziert. Sie kommt aus
dem Repository **`JanuaPort/januaport-plugins`** (JanuaPort/januaport#781).

Dieser Ordner ist ein Platzhalter, damit die Umgebungs-Matrix im
[Root-`README.md`](../README.md) vollständig bleibt.
